import { linkTeacherToSchoolAction } from "@/app/(dashboard)/school/actions"; export default function SchoolTeacherLinkCard({ successMessage, errorMessage }: { successMessage?: string; errorMessage?: string }) { return (<section className="rounded-3xl border border-indigo-200 bg-white p-6 shadow-sm"><p className="text-sm font-black text-indigo-700">👨‍🏫 ربط معلم بالمدرسة</p><h2 className="mt-1 text-2xl font-black text-slate-900">أدخل كود المعلم</h2><p className="mt-2 text-slate-500">أدخل الكود الذي أنشأه المعلم من لوحة المعلم.</p>{successMessage ? <div className="mt-4 rounded-xl bg-emerald-50 p-3 font-bold text-emerald-700">✓ {successMessage}</div> : null}{errorMessage ? <div className="mt-4 rounded-xl bg-rose-50 p-3 font-bold text-rose-700">{errorMessage}</div> : null}<form action={linkTeacherToSchoolAction} className="mt-5 flex flex-col gap-4 sm:flex-row"><input name="teacherCode" required dir="ltr" maxLength={8} placeholder="TEACHER CODE" className="min-w-0 flex-1 rounded-xl border border-slate-300 px-4 py-3 text-center font-black uppercase tracking-widest"/><button type="submit" className="rounded-xl bg-indigo-600 px-6 py-3 font-black text-white">ربط المعلم</button></form></section>); }
